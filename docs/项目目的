假设有3个进程同时到达：
- P1：需要运行 5 秒
- P2：需要运行 3 秒
- P3：需要运行 8 秒

问题：CPU 应该先运行哪个？

不同调度算法的答案不同：
- FCFS：谁先来谁先运行 → P1, P2, P3
- SJF：谁时间短谁先运行 → P2, P1, P3
- 优先级：谁优先级高谁先运行 → 取决于优先级设置
- RR：轮流运行，每人2秒 → P1(2s), P2(2s), P3(2s), P1(2s)...